-- [[ REVISED NPC HOVER HUB - FIXED DRAG & SLIDER ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local tracking = false
local heightOffset = 0
local currentNPC = nil

-- UI ScreenGui
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "NpcHoverHub"
ScreenGui.ResetOnSpawn = false

-- Tombol Kecil "Hub" (1:1)
local HubBtn = Instance.new("TextButton", ScreenGui)
HubBtn.Size = UDim2.new(0, 55, 0, 55)
HubBtn.Position = UDim2.new(0, 10, 0.5, -25)
HubBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
HubBtn.Text = "Hub"
HubBtn.TextColor3 = Color3.new(1, 1, 1)
HubBtn.Visible = false
local Corner = Instance.new("UICorner", HubBtn)

-- Main Frame
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 260, 0, 220)
MainFrame.Position = UDim2.new(0.5, -130, 0.5, -110)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 0

local MainCorner = Instance.new("UICorner", MainFrame)

-- Top Bar (DRAGGABLE FIX)
local TopBar = Instance.new("Frame", MainFrame)
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = Color3.fromRGB(45, 45, 45)

local TopCorner = Instance.new("UICorner", TopBar)
local Title = Instance.new("TextLabel", TopBar)
Title.Text = "NPC HOVER SYSTEM"
Title.Size = UDim2.new(1, -50, 1, 0)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.new(1, 1, 1)

local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -38, 0, 2)
CloseBtn.Text = "-"
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.TextColor3 = Color3.new(1, 1, 1)

-- FUNGSI DRAGGABLE UNTUK MOBILE
local function EnableDrag(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end
EnableDrag(MainFrame)

-- Toggle Button
local ToggleBtn = Instance.new("TextButton", MainFrame)
ToggleBtn.Position = UDim2.new(0, 10, 0, 55)
ToggleBtn.Size = UDim2.new(1, -20, 0, 45)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ToggleBtn.Text = "Cari & Tempel AI: OFF"
ToggleBtn.TextColor3 = Color3.new(1, 1, 1)

-- Slider UI
local SliderLabel = Instance.new("TextLabel", MainFrame)
SliderLabel.Position = UDim2.new(0, 10, 0, 115)
SliderLabel.Size = UDim2.new(1, -20, 0, 20)
SliderLabel.Text = "Tinggi/Rendah: 0"
SliderLabel.TextColor3 = Color3.new(1, 1, 1)
SliderLabel.BackgroundTransparency = 1

local SliderBar = Instance.new("Frame", MainFrame)
SliderBar.Position = UDim2.new(0, 15, 0, 145)
SliderBar.Size = UDim2.new(1, -30, 0, 12)
SliderBar.BackgroundColor3 = Color3.fromRGB(100, 100, 100)

local SliderBtn = Instance.new("TextButton", SliderBar)
SliderBtn.Size = UDim2.new(0, 25, 0, 25)
SliderBtn.Position = UDim2.new(0.5, -12, 0.5, -12)
SliderBtn.Text = ""
SliderBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)

-- LOGIKA SLIDER FIX
local isSliding = false
local function UpdateSlider(input)
    local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
    SliderBtn.Position = UDim2.new(pos, -12, 0.5, -12)
    -- Range -200 sampai 200
    heightOffset = math.floor((pos * 400) - 200)
    SliderLabel.Text = "Tinggi/Rendah: " .. tostring(heightOffset)
end

SliderBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isSliding = true
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isSliding = false
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if isSliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        UpdateSlider(input)
    end
end)

-- Minimize/Close Logic
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    HubBtn.Visible = true
end)
HubBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    HubBtn.Visible = false
end)

-- LOGIKA TRACKING KHUSUS AI (Bukan Player)
ToggleBtn.MouseButton1Click:Connect(function()
    tracking = not tracking
    ToggleBtn.Text = tracking and "Cari & Tempel AI: ON" or "Cari & Tempel AI: OFF"
    ToggleBtn.BackgroundColor3 = tracking and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(60, 60, 60)
    
    if tracking then
        local dist = math.huge
        for _, v in pairs(workspace:GetDescendants()) do
            -- Filter: Cari Humanoid yang BUKAN bagian dari player mana pun
            if v:IsA("Humanoid") and v.Parent:FindFirstChild("HumanoidRootPart") then
                local isPlayer = false
                for _, p in pairs(Players:GetPlayers()) do
                    if v.Parent == p.Character then isPlayer = true end
                end
                
                if not isPlayer then
                    local d = (LocalPlayer.Character.HumanoidRootPart.Position - v.Parent.HumanoidRootPart.Position).Magnitude
                    if d < dist then
                        dist = d
                        currentNPC = v.Parent
                    end
                end
            end
        end
    else
        currentNPC = nil
    end
end)

-- Pergerakan Karakter (Looping)
RunService.Heartbeat:Connect(function()
    if tracking and currentNPC and currentNPC:FindFirstChild("HumanoidRootPart") then
        local npcPos = currentNPC.HumanoidRootPart.CFrame
        LocalPlayer.Character.HumanoidRootPart.CFrame = npcPos * CFrame.new(0, heightOffset, 0)
        -- Stop jatuh
        LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(0,0,0)
    end
end)
